<%= form_for(@shareable) do |f| %>
  <% if @shareable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shareable.errors.count, "error") %> prohibited this shareable from being saved:</h2>

      <ul>
      <% @shareable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :is_public %><br>
    <%= f.check_box :is_public %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
